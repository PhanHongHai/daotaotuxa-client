import React, { useState } from 'react';
import moment from 'moment';
import { Card, Input, Button, Row, Col, Typography } from 'antd';

function QuickTestComponent() {
	return (
		<div className="container mb-15">
			<Row>
				<Col xs={24} md={18} className="mb-5 mt-15">
					<Typography.Title level={4}>Thi Thử</Typography.Title>
				</Col>
				<Col xs={24} md={24}>
					<Card className="phh-card-v2">
            
          </Card>
				</Col>
			</Row>
		</div>
	);
}

export default QuickTestComponent;
